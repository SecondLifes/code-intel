# CodeIntel — Sistem Analizi ve Ek Özellik Değerlendirmesi (24 Temmuz 2026)

## 1. Sistem Envanteri

| Bileşen | Durum |
|---|---|
| **Çekirdek** | `retrieval.py` (437 satır) — hibrit RRF + isim-boost + cross-encoder rerank + filtreler; panel ve MCP ortak kullanır |
| **Panel** | `panel.py` (1193 satır, FastAPI) — 30+ uç: arama, SSE sohbet, indeksleme, analitik, watch-mode, koleksiyon yönetimi |
| **Chunker** | `chunker.py` (120 satır) — tree-sitter Pascal; decl/impl/type, XML doc, çağrı çıkarımı |
| **MCP** | `mcp_server.py` — 10 tool, mcp-config.json'dan ayarlı |
| **UI** | 4 sayfa (arama, ayarlar, API testi, viewer) ~1.5K satır, framework'süz |
| **Veri** | 4 koleksiyon, ~513K nokta, 7.4GB Qdrant + 256MB chunk JSONL |
| **Kalite** | pytest 6/6 · Recall@8 %100 · MRR 1.000 (rerank açık/kapalı) · golden set 10 soru |
| **Ops** | start/stop betikleri (kendi kendini onaran venv dahil), loglar `logs\`, opsiyonel API-key |

**Mimari sağlamlığı:** Tek gerçek kaynak ilkesi iyi uygulanmış (arama mantığı yalnız `retrieval.py`'de; MCP test uçları gerçek tool fonksiyonlarını çağırıyor; profil/geçmiş/telemetri hepsi Qdrant'ın kendisinde — ayrı DB yok). Bu, ek özellik maliyetini düşüren en değerli özellik.

## 2. Güçlü Yanlar

1. **Ölçüme dayalı kültür** — eval harness'ı gerçek kararlar veriyor (rerank prior K=5 grid'le seçildi; sqlcoder→qwen3-coder değişimi gerçek testle yapılmış).
2. **Artımlı indeksleme** hash-diff'li, crash-güvenli (silme en sona), vektör türleri bağımsız yenilenebiliyor.
3. **Agent-hazır**: 10 MCP tool + REST + canlı test sayfası — Claude Code/Codex'ten gerçek kullanıma hazır.
4. **Geriye uyumluluk disiplini**: dünkü 7 özellik hiçbir koleksiyonu yeniden indeksletmeden girdi.

## 3. Teknik Borç / Riskler (ek özelliklerden ÖNCE bilinmeli)

| Sorun | Önem | Not |
|---|---|---|
| `src/indexer.py` **ölü/tuzak kod** | Orta | Eski Phase-3 betiği; adsız default vektörle koleksiyon açıyor — bugünkü dense/sparse şemasıyla **uyumsuz**, yanlışlıkla çalıştırılırsa bozuk koleksiyon üretir. Silinmeli veya `tests/manual`'a taşınmalı. |
| Golden set 10 soru, yalnız unidac | Orta | %100'e doymuş durumda — artık iyileşmeleri ölçemez. Analitikteki sıfır-sonuç sorgular doğal büyütme kaynağı. |
| `panel.py` 1193 satır monolit | Düşük | Çalışıyor; ama koleksiyon-yönetimi / indeksleme / sohbet ayrı router dosyalarına bölünürse ek özellik maliyeti düşer. |
| `data/` içinde test artıkları | Düşük | `chunks-testA-renamed/testB/__testcalls.jsonl` — temizlenebilir. |
| mcp-config `default_collections: ["unidac"]` | Düşük | 4 koleksiyon varken agent varsayılanı tek koleksiyon — bilinçli mi gözden geçirin. |
| Commit edilmemiş büyük değişiklik seti | Orta | Dünkü 7 paket + betik onarımları hâlâ working tree'de. |

## 4. Ek Özellik Değerlendirmesi

Puanlama: Etki (kullanıcı/agent değeri) ve Emek 1-5 · **Kaldıraç = Etki/Emek**.

### A. Arama Kalitesi ve Kapsam

| # | Özellik | Etki | Emek | Değerlendirme |
|---|---|---|---|---|
| **A1** | **Çok dilli chunker** (C++, C#, Java, Python, JS/TS…) | 5 | 3 | **En stratejik hamle.** Sistemi "Delphi aracı"ndan "kod-intel platformu"na çıkarır. Altyapı hazır: `tree-sitter-language-pack` zaten kurulu, `LANG_EXT` tablosu zaten 12 dili tanıyor, embedding/arama/UI dil-bağımsız. Gereken: dil başına tree-sitter sorgusu (fonksiyon/sınıf düğümleri) + isim/doc çıkarımı. Dil başına ~1 gün; ilk hedef olarak C++ veya C# önerilir (Delphi ekosistemine komşu). |
| A2 | LLM sorgu genişletme (TR sorguyu EN kod terimlerine çeviren hızlı rewrite) | 3 | 2 | Türkçe sorgularda dense model iyi ama BM25 kolu kör kalıyor. Ollama'daki fast model ile 1 çağrı; eval ile kanıtlanabilir. Rerank gibi opsiyonel anahtar olmalı. |
| A3 | Eşleşen satır vurgusu (sonuç kartında sorgu terimlerinin geçtiği satırlara işaret) | 3 | 2 | Salt UI işi; uzun chunk'larda "neden bu sonuç geldi" sorusunu anında yanıtlar. |
| A4 | MMR çeşitlilik (ilk sayfada aynı dosyadan N'den fazla sonuç bastırma) | 2 | 2 | Dedup sonrası hâlâ tek dosya baskınlığı olabiliyor; küçük ama hoş. |

### B. Kod Anlama Derinliği (agent'lar için)

| # | Özellik | Etki | Emek | Değerlendirme |
|---|---|---|---|---|
| **B1** | **Tip hiyerarşisi** (`class TFoo = class(TBar)` → kalıtım ağacı; `get_type_hierarchy` MCP tool + UI) | 4 | 3 | Delphi'de sınıf hiyerarşisi kod anlamanın bel kemiği. `declType` chunk'ları zaten indeksli; ata sınıf adı regex'le çıkar, `_link_call_graph` desenine birebir oturur (index-time hesap, payload'a yaz). |
| **B2** | **uses-bağımlılık grafiği** (unit-düzeyi import ağı; panelde mermaid görselleştirme) | 4 | 3 | "Bu dosya nelere bağımlı / bunu kim kullanıyor" — refactor ve etki analizi için. Chunker'a `uses` yakalama + koleksiyon-düzeyi graf ucu. |
| B3 | Kod metrikleri (chunk başına satır/karmaşıklık/parametre sayısı; koleksiyon özeti) | 2 | 2 | review_code'a bağlam olur; panelde "en büyük 20 metod" gibi listeler. |

### C. RAG / Sohbet

| # | Özellik | Etki | Emek | Değerlendirme |
|---|---|---|---|---|
| **C1** | **Derin araştırma modu** (çok adımlı: ara → ilgili dosyayı `read_unit`'le oku → gerekirse ikinci arama → sentezle; adımları UI'da göster) | 5 | 4 | Tek atımlık RAG'in tavanını aşar; "bu kütüphanede transaction yönetimi nasıl kurgulanmış?" gibi mimari soruları gerçekten yanıtlar. Mevcut tool'lar yapı taşı olarak hazır — orkestrasyon döngüsü + UI akışı gerek. |
| C2 | Sohbet kalıcılığı + dışa aktarma (oturumlar listesi, md indir) | 3 | 2 | Geçmiş şu an sekme-ömürlü. localStorage yeter, sunucu değişikliği gerekmez. |
| C3 | Yanıt önbelleği (aynı soru+koleksiyon+model → anında yanıt) | 2 | 1 | explain cache deseninin aynısı; `_search_log`'a benzer bir koleksiyona. |

### D. Operasyon / Kurumsallık

| # | Özellik | Etki | Emek | Değerlendirme |
|---|---|---|---|---|
| **D1** | **Zamanlanmış otomatik yedek** (export .gz → `backups\`, N kopya rotasyon; watcher thread'ine eklenir) | 4 | 2 | 7.4GB Qdrant verisinin tek kopyası var; export altyapısı hazır olduğundan ucuz sigorta. |
| D2 | Açılışta otomatik başlatma (Görev Zamanlayıcı kaydı yapan `install-autostart.ps1`) | 3 | 1 | start-system artık sağlam; tek satır schtasks kaydı. |
| D3 | Log rotasyonu + panelde son loglar sekmesi | 2 | 2 | `logs\` büyür; basit boyut-tabanlı rotasyon. |
| D4 | Çok kullanıcılı erişim (LAN'a açma: API-key UI'sı, salt-okunur mod) | 2 | 3 | Yalnız ekip kullanımı planlanıyorsa; şimdilik erteleme adayı. |

### E. Kalite Güvence

| # | Özellik | Etki | Emek | Değerlendirme |
|---|---|---|---|---|
| **E1** | **Golden set büyütme** (10→50+; sıfır-sonuç sorgulardan aday üretme; koleksiyon başına set; eval'e koleksiyon matrisi) | 4 | 2 | Metrikler doymuş durumda — bundan sonraki her arama iyileştirmesi ölçülemez halde. Diğer tüm arama işlerinin ön koşulu. |
| E2 | Eval A/B komutu (`eval.py --compare` iki konfigi yan yana) | 2 | 1 | Rerank/K/ağırlık deneylerini tek komutla raporlar. |
| E3 | Chunker kenar-durum testleri (iç içe proc, generics, anonim metod, class helper) | 3 | 2 | Çok dilli chunker'a (A1) girmeden önce Pascal tarafını sağlamlaştırır. |

## 5. Önerilen Yol Haritası

**Sprint 1 — "Ölç ve genişlet" (1 hafta):**
`E1` golden set büyütme → `A2` sorgu genişletme (eval'le kanıtla) → `D1` otomatik yedek → `D2` otomatik başlatma → teknik borç temizliği (indexer.py, test JSONL'leri, commit).

**Sprint 2 — "Platform hamlesi" (1-2 hafta):**
`A1` çok dilli chunker — önce C# veya C++ tek dil pilotu, LANG_EXT + ayarlar sayfasında dil seçimi, kendi golden set'iyle.

**Sprint 3 — "Derinlik" (1-2 hafta):**
`B1` tip hiyerarşisi + `B2` uses grafiği (ikisi aynı index-time desenle) → `C1` derin araştırma modu (B1/B2 tool'ları da orkestrasyona girer → çarpan etkisi).

**Gerekçe:** Sırayı belirleyen üç ilke — (1) önce ölçüm kapasitesi (E1 olmadan A serisinin başarısı kanıtlanamaz), (2) veri güvenliği erken (D1 ucuz sigorta), (3) çarpan etkisi (B1+B2, C1'in yapı taşları; A1 tüm sistemin pazarını büyütür).

---
*Bu analiz canlı sistem taramasıyla üretildi: 4 koleksiyon/513K nokta doğrulandı, tüm kaynak dosyalar okundu, metrikler son eval koşusundan alındı.*
