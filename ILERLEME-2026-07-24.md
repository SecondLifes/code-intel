# CodeIntel — Birleşik Yol Haritası Uygulama Raporu (24 Temmuz 2026)

`MASTER-ANALIZ-2026-07-24.md`'deki birleşik plan uygulandı. **6 commit, Faz 0 + Faz 1 tamamen, Faz 2'nin ilk büyük dilimi bitti.** pytest 6/6 · eval 60 soru: Recall@8 %85 (rerank ile %92), MRR 0.647, p50 21ms. Panel çalışır durumda.

## Commit'ler
| Commit | İçerik |
|---|---|
| `44ac1a1` | Dünkü pro-segment paketi (rerank, filtreler, SSE sohbet, analitik, watch, 10 MCP tool) + betikler |
| `d82db81` | **Faz 0**: P0 düzeltmeleri + bellek-sınırlı indeksleme |
| `5b920ac` | **Faz 0**: 60 soruluk çok-koleksiyonlu golden set + nDCG/p50/p95/--compare |
| `df7a1e1` | **Faz 1**: 👍/👎 geri bildirim + sıralama açıklanabilirliği (ⓘ) |
| `8af443b` | **Faz 1**: kopya kod raporu, rotasyonlu yedekler, akışlı export, autostart |
| `285f65f` | **Faz 2**: sembol grafiği (kalıtım/interface) + 2 yeni MCP tool (toplam 12) |

## Faz 0 — Temel (tamam)
- **Yarış durumu** düzeltildi (index_start artık diffing/linking fazlarını da sayıyor)
- **Merge ID çakışmaları** artık raporlanıyor (sessiz veri kaybı riski kapandı)
- **"Tam kod" dürüstlüğü**: `truncated` bayrağı + kesikse diskten gerçek dosyadan tam okuma (test: 4000 → 7127 karakter)
- **Bellek**: diffing artık vektörleri RAM'e almıyor; `_link_call_graph` 3-geçişli, yalnız DEĞİŞEN ilişkiyi yazıyor (Jedi-ölçeği OOM riski kapandı)
- **Config tekleşti**: mcp-config.json'daki URL'ler artık gerçekten etkili (env > dosya > varsayılan)
- Ölü `indexer.py` silindi; test artıkları temizlendi
- **Eval 10→60 soru** (4 koleksiyon), nDCG + p50/p95 + `--compare`. İlk gerçek bulgu: rerank Recall'u %85→%92 çıkarıyor (eski doymuş sette görünmüyordu)

## Faz 1 — Hızlı kazanımlar (tamam)
- **Geri bildirim**: her sonuç kartında 👍/👎 → telemetriye; Analitik'te sayılar + "alakasız işaretlenenler" listesi. Otomatik sıralamaya BİLEREK bağlanmadı (overfit uyarısı) — golden set aday kaynağı
- **Sıralama açıklaması**: her sonuçta ⓘ (anlamsal/kelime ham sırası, isim boost, rerank olasılığı)
- **Kopya kod raporu**: koleksiyon-geneli benzerlik taraması (kayıtlı vektörlerle, embedding hesaplamadan) + Ayarlar'da kart. RESTRequest4Delphi testi gerçek kopyaları yakaladı
- **Yedekler**: günlük otomatik (≤50K nokta + iç kayıtlar; büyükler elle), son-3 rotasyon, .tmp-sonra-adlandır; Ayarlar'da kart. Export/import artık akışlı (RAM'de kopya yok) ve iç koleksiyonları da taşıyabiliyor
- **Autostart**: `tools/install-autostart.ps1` (oturum açılışında sistem sessizce kalkar; `-Uninstall` ile kaldırılır)

## Faz 2 — Sembol grafiği (ilk dilim tamam)
- `_symbol_graph` AYRI kenar koleksiyonu (payload'a liste gömme yaklaşımı bilinçli reddedildi — ölçek kararı)
- Tip bildirimlerinden kalıtım/interface kenarları: **unidac 1928 · Jedi 17.388 · mORMot2 2.990 · RESTRequest4Delphi 31 kenar** — canlı kuruldu, her indekslemede otomatik tazelenir
- Yeni MCP tool'ları (12 oldu): **`get_type_hierarchy`** (atalar zinciri + 2 seviye altlar + interface'ler; korpus-dışı atalar bayraklı) ve **`find_references`** (tanımlar + çağıranlar + metinsel izler)
- Doğrulama: `TCRConnection` → tüm sağlayıcı bağlantıları altında listelendi; `TJclAbstractContainer` → `TInterfacedObject` (korpus dışı) atası doğru çözüldü

## Kalanlar (Faz 2 devamı + Faz 3)
1. `get_context_pack` MCP tool'u (token bütçeli agent bağlam paketi) — sembol grafiği artık hazır, sıradaki iş
2. Derin araştırma sohbet modu (çok adımlı orkestrasyon)
3. Hiyerarşinin panel UI'sına işlenmesi (İlişkiler kutusuna "Üst/alt sınıflar" grubu)
4. Faz 3: Git provenance + staging/alias'lı atomik indeks + etki analizi
5. Sorgu genişletme pilotu (A2 — eval'le kanıtlanacak, denenmedi)

## Notlar
- `uses`-bağımlılık kenarları bu dilime girmedi (chunk'lar unit başlıklarını içermiyor; kaynak dosya okuma gerektirir — Faz 2 devamına not edildi)
- Panel API'sinde artık 12 MCP test ucu + `/api/symbols/rebuild`, `/api/duplicates/*`, `/api/backup/*`, `/api/feedback`, `/api/analytics` mevcut; Swagger `/docs`'ta hepsi görünür
